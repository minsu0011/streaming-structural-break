# 관측이 도착할 때만 상태 갱신

새 관측을 `StreamingFeatureState`에 넣고 제한된 과거 상태에서 특징을 계산합니다. 통계·AR 기준선과 NEXT/NEXT2 분산·의존성 경로가 이를 순차 점수로 바꿉니다.

학습과 추론은 같은 정보 가용 시점을 따라야 합니다. 시계열 단위 분할과 최종 독립 평가는 별도 경계입니다. 합성 테스트는 작은 입력의 연산·반응을 확인하고, 공식 입력 프로토콜 검사는 별도 제공되는 `vendor/official/runner.py`가 필요합니다.
